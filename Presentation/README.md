# Presentation Materials

## 📹 Video Presentation

The video file (`Multi_Agent_Sports_Intelligence_System_By_Tausif.mp4`, 119MB) is too large for GitHub's 100MB limit and is stored locally only.

### Alternative Options to Share the Video:

1. **Google Drive / Dropbox**
   - Upload the video to cloud storage
   - Share the link publicly
   
2. **YouTube (Recommended for Portfolio)**
   - Upload as unlisted video
   - Add link to README

3. **Git LFS (Git Large File Storage)**
   ```bash
   # If you want to use Git LFS
   git lfs install
   git lfs track "*.mp4"
   git add .gitattributes
   git add Presentation/*.mp4
   git commit -m "Add video with Git LFS"
   git push
   ```

## 📄 PDF Presentation

The PDF presentation (`Multi-Agent-Sports-Intelligence-System-By-Tausif.pdf`) is included in the repository and available on GitHub.

---

**Note:** The video file is excluded from git tracking via `.gitignore` to comply with GitHub's file size limits.
